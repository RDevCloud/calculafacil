export function generateTitle(toolName: string): string {
  return `${toolName} online gratis | CalculaFácil`;
}

export function generateDescription(toolName: string, action: string): string {
  return `Calcula ${action} rápidamente con nuestra calculadora de ${toolName} en español. Fácil, rápida y precisa.`;
}